# pico-basic ユーザーマニュアル

pico-basic は、Raspberry Pi Pico 2 (RP2350) 上で動作する、SHARP X1 turbo (CZ-8FB02) 互換のネイティブ BASIC インタプリタです。1980年代のマイコンの操作感そのままに、現代の強力なハードウェア機能（カラー液晶、SDカード、GPIO制御）を扱うことができます。

## 1. 基本的な使い方

pico-basic には、入力したコマンドが即座に実行される「ダイレクトモード」と、行番号を付けてメモリに記憶させる「プログラムモード」の2つの使い方が存在します。

### ダイレクトモード（即時実行）
行番号（数字）を付けずにコマンドを入力し、Enterキーを押すとすぐに実行されます。計算やハードウェアの簡単なテストに便利です。
```basic
PRINT 10 * 20
200
Ready
```

### プログラムモード
行番号（1〜65535）を先頭に付けて入力すると、コマンドは実行されずにメモリに記憶されます。
```basic
10 PRINT "HELLO, PICO!"
20 GOTO 10
RUN
```
* **`LIST`**: 記憶されたプログラムを画面に表示します。
* **`RUN`**: 記憶されたプログラムを行番号の小さい順に実行します。**実行開始時に変数と配列は初期化されます**（`DIM` を含むプログラムを繰り返し実行できます）。
* **`NEW`** (または **`CLEAR`**): メモリ上のプログラムと変数をすべて消去します。
* **`AUTO [開始番号 [, 刻み]]`**: 行番号を自動で表示しながら入力できるモードに入ります。`AUTO` だけなら 10 から 10 刻み、`AUTO 100, 5` なら 100 から 5 刻みです。行を入力して Enter を押すと次の番号が自動で出ます。**何も入力せずに Enter**、または **Ctrl-C** で AUTO モードを抜けます。
  ```basic
  AUTO
  10 PRINT "HELLO"     ← 「10 」は自動、"PRINT ..." だけ入力
  20 GOTO 10           ← 「20 」も自動
  30                   ← 何も打たずに Enter で終了
  ```

---

## 2. 変数とデータ型

変数名は「英字1文字」または「英字1文字＋数字1文字」（例: `A`, `B1`）が使用できます。末尾の記号によって扱うデータの種類が変わります。

| 型 | 記号 | 説明 | 例 |
| :--- | :---: | :--- | :--- |
| **単精度実数** | `!` | デフォルトの型です（記号省略可）。小数を含む数値を扱います。 | `A = 3.14` または `A! = 3.14` |
| **整数** | `%` | 16ビットの符号付き整数（-32768 〜 32767）を扱います。高速な処理が可能です。 | `X% = 256` |
| **倍精度実数** | `#` | より精度の高い小数を扱います。 | `PI# = 3.14159265` |
| **文字列** | `$` | 文字の並び（最大255文字）を扱います。ダブルクォーテーション `"` で囲みます。 | `N$ = "PICO 2"` |

*※配列を使用する場合は、事前に `DIM A(10)` のように宣言が必要です。*

### 配列（数値・文字列）
`DIM` で宣言すれば、数値配列も**文字列配列**も使えます。1 次元・2 次元に対応します。

```basic
10 DIM NM$(3), SC(3)        ' 文字列配列と数値配列
20 NM$(0) = "ALICE" : SC(0) = 90
30 PRINT NM$(0), SC(0)
40 DIM BOARD$(2, 2)         ' 2 次元の文字列配列（3×3）
50 BOARD$(1, 1) = "O"
```
未代入の要素は、数値なら `0`、文字列なら空文字列 `""` です。

### 演算子と優先順位
式では次の演算子が使えます（上ほど優先順位が高い＝先に計算される）。

| 優先 | 演算子 | 種類 |
| :---: | :--- | :--- |
| 高 | `^` | べき乗 |
| ↑ | `*` `/` | 乗除 |
| ｜ | `\` | 整数除算（両辺を整数にして割る。`7\2` → `3`） |
| ｜ | `MOD` | 剰余（`10 MOD 3` → `1`） |
| ｜ | `+` `-` | 加減（`+` は文字列連結にも使用） |
| ｜ | `=` `<>` `<` `>` `<=` `>=` | 比較（真は `1`、偽は `0`） |
| ｜ | `NOT` | ビット補数（単項） |
| ｜ | `AND` | ビット積 |
| ｜ | `OR` | ビット和 |
| 低 | `XOR` | ビット排他的論理和 |

数値は **`&H`（16進）/ `&B`（2進）** でも書けます。`LIST` でも表記が保たれます。
```basic
SOUND 7, &H3E        ' ミキサー: トーン A のみ有効（10進の 62）
POKE &H9C40, &B1010  ' ビット指定が読みやすい
```

`AND` / `OR` / `NOT` / `XOR` は**ビット演算**です。効果音のミキサー計算やゲームのフラグ管理に使えます。
```basic
PRINT 6 AND 3        ' → 2（ビット積）
PRINT 5 OR 2         ' → 7（ビット和）
PRINT 255 AND 15     ' → 15（下位 4bit の取り出し）
```
比較は `1`/`0` を返すので、条件の組み合わせにも使えます。
```basic
IF A = 3 AND B = 7 THEN PRINT "BOTH"
IF X < 0 OR X > 99 THEN PRINT "OUT OF RANGE"
```
> `NOT` はビット補数（`NOT 0` は `-1`）です。条件の否定は `NOT` ではなく `X = 0` のように比較で書いてください。

### 組み込み関数

| 分類 | 関数 |
| :--- | :--- |
| 数値 | `ABS` `INT` `SGN` `SQR` `SIN` `COS` `TAN` `LOG` `EXP` `RND(n)` |
| 文字列 | `LEN` `MID$` `LEFT$` `RIGHT$` `CHR$` `ASC` `VAL` `STR$` `INSTR([開始,]文字列,検索語)` `STRING$(n,文字)` `SPACE$(n)` `HEX$(n)` |
| ハードウェア | `PEEK(addr)` `POINT(x,y)`（画面の色番号。画面外は -1） `TOUCH(n)` `TIMER`（起動からのミリ秒。括弧なし） |

```basic
PRINT INSTR("HELLO", "LL")   ' → 3（見つからなければ 0）
PRINT STRING$(5, "*")        ' → *****
PRINT HEX$(255)              ' → FF
T = TIMER : GOSUB 100 : PRINT TIMER - T   ' 処理時間の計測
IF POINT(X, Y) = 4 THEN PRINT "HIT"       ' 画面の色で衝突判定
```

### ユーザー定義関数（`DEF FN`）
よく使う計算式に名前を付けられます。名前は `FN` で始め、引数は 1 つです。

```basic
10 DEF FNSQ(X) = X * X            ' 2 乗
20 DEF FNC(F) = (F - 32) * 5 / 9  ' 華氏→摂氏
30 PRINT FNSQ(5)                  ' → 25
40 PRINT FNC(212)                 ' → 100
```
`DEF` は実行された時点で有効になるので、使う前に一度通しておきます。引数名は関数の中だけで有効で、同名の変数があっても壊れません。

---

## 3. 制御ステートメント

プログラムの流れを制御するための基本的なコマンドです。

| コマンド構文 | 動作の説明 |
| :--- | :--- |
| **`IF`** 条件 **`THEN`** 処理 **`[ELSEIF`** 条件 **`THEN`** 処理 …**`] [ELSE`** 処理**`]`** | 条件を満たした時に `THEN` 以降を実行します。`ELSEIF` を並べると、上から順に条件を試し、最初に成立したものだけを実行します。どれも成立しなければ `ELSE` 以降を実行します。 |
| **`IF`** 条件 **`GOTO`** 行番号 / **`IF`** 条件 **`GOSUB`** 行番号 | 分岐する場合は `THEN` を省略できます。`IF A=3 GOTO 100` は `IF A=3 THEN GOTO 100` と同じです（`*`ラベルも可）。 |
| **`FOR`** 変数=初期値 **`TO`** 終値 **`[STEP`** 増減値**`]`** | 変数を変化させながら、`NEXT` までの処理を繰り返します。 |
| **`NEXT`** **`[`** 変数 **`]`** | `FOR` ループの終端を示します。 |
| **`GOTO`** 行番号\|`*`ラベル | 指定した行番号（または行ラベル）へ無条件にジャンプします。 |
| **`GOSUB`** 行番号\|`*`ラベル | 指定した行番号（または行ラベル）のサブルーチンへジャンプします。 |
| **`RETURN`** | サブルーチンから、`GOSUB` を呼んだ**すぐ次の文**へ戻ります。 |
| **`REPEAT`** 処理 **`UNTIL`** 条件 | 条件を満たすまで、`REPEAT` と `UNTIL` の間の処理を繰り返します（後判定ループ）。 |
| **`REM`** コメント / **`'`** コメント | その行の以降を注釈として無視します。`REM` は行の途中（`:` の後）にも、`'` は文の末尾にも置けます。 |

これらは**文単位で復帰**します。1行に `:` でまとめて書けます。
```basic
10 GOSUB 100 : PRINT "BACK"      ' RETURN 後に PRINT "BACK" が実行される
20 FOR J=1 TO 3 : PRINT J : NEXT J  ' 単一行の FOR…NEXT も正しくループする
30 REPEAT : I=I+1 : UNTIL I>=3   ' 単一行の REPEAT…UNTIL も可
```

### 行ラベル

行番号の代わりに、`*` で始まる**行ラベル**でジャンプ先を示せます。ラベルは行の先頭に `*名前` と書いて定義し、`GOTO` / `GOSUB` / `IF … THEN` から `*名前` で参照します（大文字・小文字は区別しません）。

```basic
10 X = 5
20 IF X > 0 THEN *POSITIVE
30 PRINT "ZERO OR NEG" : END
40 *POSITIVE
50 PRINT "POSITIVE"
```

`IF 条件 THEN *ラベル`（および `THEN 行番号`）は、その行番号／ラベルへの `GOTO` として働きます。

---

## 4. 画面・グラフィック制御

pico-basic は、仮想VRAMを通じてSPIディスプレイに高速な描画を行います。

### システム・テキスト制御
* **`CLS [n]`**: 画面を消去します。
* **`CONSOLE 開始行, 行数`**: テキストのスクロール領域を指定した行範囲に制限します（引数なしで全画面に戻す）。
* **`WIDTH 文字数 [, 行数]`**: 画面の文字数・行数を指定します。本機のフォントは 8×8 固定のため **40×30 のみ**対応し、それ以外はエラーになります。

### 図形描画
グラフィックコマンドは `WINDOW` 命令で設定された座標系に基づきます。

* **`WINDOW (xs,ys)-(xe,ye), (x1,y1)-(x2,y2)`**: 画面上の矩形 `(xs,ys)-(xe,ye)` に、自分の使いたい座標 `(x1,y1)-(x2,y2)` を割り当てます。以降 `PSET` / `LINE` / `CIRCLE` / `PAINT` / `POLY` / `GET@` / `PUT@` の座標はこの座標系で指定します。引数なしの `WINDOW` で画面座標に戻ります。
  ```basic
  WINDOW (0,239)-(319,0), (0,0)-(319,239)  ' Y 軸を反転（数学の座標系）
  WINDOW (0,0)-(319,239), (0,0)-(1,1)      ' 0.0〜1.0 で指定できる座標系
  WINDOW                                    ' 画面座標へ戻す
  ```
* **`PSET (x, y), c`**: 指定した座標に、カラーコード `c` で点を打ちます。
* **`LINE (x1,y1)-(x2,y2), c [, B|BF]`**: 直線を描画します。末尾に `B` を付けると枠だけの長方形、`BF` を付けると塗りつぶした長方形になります。
  ```basic
  LINE (20,20)-(120,80), 14, B    ' 枠だけの長方形
  LINE (140,20)-(240,80), 12, BF  ' 塗りつぶした長方形
  ```
* **`CIRCLE (x,y), r, c, f, s, e`**: 円や楕円、扇形を描画します。
* **`POLY (x,y), r, c, n [, skip [, angle]]`**: 正多角形や星形を描画します。`n` は頂点数（3以上）、`skip` は何個先の頂点と結ぶか（1=多角形、2以上=星形、既定 1）、`angle` は最初の頂点の角度（度、既定 -90＝真上）です。
  ```basic
  POLY (80,180), 40, 10, 6      ' 正六角形
  POLY (200,180), 40, 13, 5, 2  ' 五芒星
  ```
* **`PAINT (x,y), c, b`**: 指定した境界色 `b` で囲まれた領域を、色 `c` で塗りつぶします。

### イメージ操作
* **`GET@ (x1,y1)-(x2,y2), A`**: 画面の指定した長方形領域を、配列変数 `A` に画像データとして取り込みます。配列は先頭2要素を幅・高さに使うため、`DIM` は「画素数＋2」以上が必要です。
  > **大きさの上限:** 配列ヒープの都合で **最大 1,534 画素（正方形なら 39×39 程度）** までです。
  ```basic
  10 DIM A(1000)
  20 CIRCLE (40,40),12,14
  30 GET@ (28,28)-(52,52), A
  40 PUT@ (200,150), A
  ```
* **`PUT@ (x,y), A [, mode]`** / **`PUT@ (x1,y1)-(x2,y2), A [, mode]`**: 取り込んだ配列変数 `A` の画像を描画します。
  第2点 `-(x2,y2)` を付けると、その矩形に合わせて**拡大縮小**します。`mode` は描画方法を選びます。
  | mode | 動作 |
  | :---: | :--- |
  | 0 | PSET（そのまま上書き。既定） |
  | 1 | OR（画面と論理和） |
  | 2 | AND（画面と論理積） |
  | 3 | **XOR**（同じ絵を2回置くと消える。スプライト向き） |
  | 4 | PRESET（色を反転して上書き） |
  ```basic
  10 DIM A(200)
  20 CIRCLE (8,8),6,15
  30 GET@ (0,0)-(16,16), A
  40 PUT@ (100,100), A, 3   ' XOR で描く
  50 PUT@ (100,100), A, 3   ' もう一度で消える
  ```

---

### 実行中のプログラムを止める（Ctrl-C）

`GOTO` による無限ループなど、実行が終わらないプログラムは **Ctrl-C** で中断できます。鳴っている演奏も同時に止まります。
中断すると `Break in 行番号` が表示され、`Ready` に戻ります。変数やプログラムは保持されるため、`LIST` や `PRINT` で状態を確認できます。

```basic
10 IF TOUCH(2) THEN PSET (TOUCH(0), TOUCH(1)), 15
20 GOTO 10
RUN
（Ctrl-C を押す）
Break in 10
Ready
```

---

## 5. サウンド・ハードウェア制御

Raspberry Pi Pico 2の機能を活かした拡張コマンドです。

* **`BEEP [n]`**: スピーカーからブザー音を鳴らします。
* **`MUSIC "MML文字列"[, "MML文字列", "MML文字列"]`** / **`PLAY ...`**: MML（Music Macro Language）で音楽を演奏します。カンマで区切って最大3声まで指定でき、同時に鳴らして和音になります。`PLAY` は `MUSIC` と同じ命令です。
  ```basic
  PLAY "CDEFG"                 ' 単音のメロディ
  PLAY "O4CEG", "O4EGB", "O5C" ' 3声の和音
  ```
  使える記号: `A`〜`G`（音名）、`R`（休符）、`#`/`+`/`-`（半音）、`O1`〜`O8`（オクターブ）、`>` `<`（オクターブ増減）、`L1`〜`L64`（音の長さ）、`T32`〜`T255`（テンポ）、`V0`〜`V15`（音量）、`.`（付点）。`V` は声ごとに独立しており、他の声や `BEEP` の音量には影響しません。

  音量を指定しない場合は **8**（最大 15 の約半分）で鳴ります。全体の既定値を変えたい場合は `src/hal_sound.h` の `HAL_SOUND_DEFAULT_VOLUME` を変更してください。

  **演奏は非同期です。** `PLAY` / `MUSIC` は鳴り終わるのを待たずに次の行へ進むため、音楽を鳴らしながら描画や計算ができます。
  ```basic
  10 PLAY "O5CDEFGAB"
  20 FOR I = 0 TO 319
  30 PSET (I, 120), 15
  40 NEXT I
  ```
  演奏を途中で止めるには **Ctrl-C** または `NEW` を使います（`Ready` の状態でも、プログラム実行中でも止まります）。続けて `PLAY` を呼ぶと、前の演奏の後ろに繋がって鳴ります。

  > 一度に非同期で先読みできるのは**約 128 音**までです。それを超える長い曲は、音符が消化されるまで `PLAY` が待ちます（曲は正しく鳴りますが、その間は次の行に進みません）。
* **`SOUND レジスタ, 値`**: PSG 音源（AY-3-8910 相当）のレジスタに直接書き込みます。`PLAY` とは別系統で、書き込むと**明示的に止めるまで鳴り続けます**（`PLAY` / `MUSIC` / `BEEP` / `NEW` / Ctrl-C で止まります）。
  | レジスタ | 内容 |
  | :---: | :--- |
  | 0/1, 2/3, 4/5 | チャンネル A/B/C のトーン周期（12bit。下位8bit＋上位4bit） |
  | 6 | ノイズ周期（0〜31） |
  | 7 | ミキサー。bit0-2＝トーン、bit3-5＝ノイズの 有効(0)/無効(1) |
  | 8/9/10 | チャンネル A/B/C の音量（0〜15） |
  | 11/12 | エンベロープ周期（16bit）。周期 = 2MHz÷(256×値) |
  | 13 | エンベロープ形状（下位4bit）。R8-10 の bit4 を立てた ch に適用 |

  **トーン周期の求め方:** `周期 = 2000000 ÷ (16 × 出したい周波数)`。例えば 440Hz なら `2000000÷(16×440) ≒ 284`。
  レジスタは 8bit なので、`SOUND 0, P` と書けば周期の下位 8bit が自動的に入ります（上位は `P/256`）。
  ```basic
  10 P = 2000000 / (16 * 440)   ' 440Hz の周期 ≒ 284
  20 SOUND 0, P                  ' 下位 8bit（自動でマスクされる）
  30 SOUND 1, P / 256            ' 上位 4bit
  40 SOUND 8, 15                 ' チャンネル A 音量 最大
  50 SOUND 7, 62                 ' ミキサー: トーン A だけ有効（bit0=0）
  ' ここで鳴り続ける。止めるには BEEP や PLAY、CLEAR など
  ```
  ノイズを鳴らすには R6（周期）と R7 のノイズビットを設定します。効果音向けです。

  **エンベロープ**を使うと音量が自動で変化します。R8〜R10 に **16**（bit4）を足したチャンネルが対象になり、R13 で形状を選びます（`9`=減衰して消える、`8`=繰り返し減衰、`14`=三角波の繰り返し 等）。
  ```basic
  10 P = 2000000 / (16 * 440)
  20 SOUND 0, P
  30 SOUND 1, P / 256
  40 SOUND 11, 0 : SOUND 12, 40   ' エンベロープ周期（大きいほどゆっくり）
  50 SOUND 13, 9                   ' 形状 9: 減衰して消える
  60 SOUND 8, 16                   ' ch A をエンベロープ音量に（bit4=16）
  70 SOUND 7, 62                   ' トーン A 有効
  ' 音量が自動で減衰してフェードアウトする
  ```
* **`POKE アドレス, 値`** / **`PEEK(アドレス)`**: 本機の**論理メモリ**（64KB）に 1 バイトを読み書きします。`POKE` は値を下位 8bit（0〜255）にして書き込み、`PEEK` はそのバイト（0〜255）を返します。アドレスは 0〜65535 です。
  ```basic
  POKE 40000, 65 : PRINT PEEK(40000)   ' → 65
  ```
  > **注意**: これは X1 実機の物理メモリマップではなく、本実装が内部で使う論理メモリです。プログラムや変数の領域（低位アドレス・`&H8000` 以降の変数域）を上書きすると動作が壊れます。安全に使うなら 40000〜50000 付近の未使用域を目安にしてください（あくまで学習・実験用です）。
* **`GPIO PIN, MODE, VALUE`**: Pico 2のハードウェアピン（GPIO）を直接制御します。
* **`GET 変数名`**: キーボードからの入力を**待たずに**1文字取得します（押されていなければ空文字列／0）。ゲームのリアルタイム入力に使います。文字変数（`K$`）には文字そのもの、数値変数（`K`）には文字コードが入ります。
* **`FILES`**: SDカードに保存されているファイルの一覧を表示します。

---

## 6. 基本仕様および注意事項

Pico-HuBASICを扱う上での重要な仕様と、当時のHu-BASICとの共通点・相違点について説明します。

### 6.1 数値の表示形式について
実数型変数（デフォルトの `A` など）を `PRINT` した際、`10.0000` のように小数点以下が表示されるのは正常な動作です。
* **実数型**: 内部的に浮動小数点数として処理されるため、整数値であっても実数フォーマットで出力されます。
* **整数型**: 小数点を表示したくない場合は、変数名の後ろに `%` を付けて整数型変数（例: `A%`）を使用してください。

### 6.2 大文字・小文字の扱い
本インタプリタは、命令語および変数名において**大文字と小文字を区別しません。**
* `PRINT A`, `print a`, `Print A` はすべて同じ意味として解釈されます。
* ただし、ダブルクォーテーション `"` で囲まれた文字列内（例: `"Apple"` と `"apple"`）は厳密に区別されます。

---

## 7. エラーメッセージ一覧

プログラムの実行中に問題が発生した場合、`Error <コード> in line <行番号>: <詳細>` の形式で表示されます（ダイレクトモードでは行番号なし）。主なコードは次のとおりです。コード表に無いエラーは番号なしで詳細メッセージだけが表示されます。

| コード | メッセージ | 主な原因 |
| :---: | :--- | :--- |
| **2** | `Syntax error` | コマンドの綴り間違いや、文法が間違っています。 |
| **5** | `Illegal function call` | コマンドに渡した数値（引数）が、許容される範囲を超えています。 |
| **6** | `Overflow` | 計算結果が変数の扱える最大値・最小値を超えました。 |
| **7** | `Out of memory` | メモリ（フリーエリア）が不足しています。 |
| **8** | `Undefined line number` | 指定した行番号が存在しません。 |
| **9** | `Subscript out of range` | 配列変数の指定が `DIM` で宣言したサイズを超えています。 |
| **10** | `Duplicate definition` | すでに宣言済みの配列を再度 `DIM` しようとしました。 |
| **13** | `Type mismatch` | 数値と文字列の型が一致していません。 |
| **26** | `File not found` | ファイルがSDカード上に見つかりません。 |
| **34** | `Reserved feature` | 予約されている（未対応の）機能を使おうとしました。 |

---

## 8. サンプルプログラム

`samples/` フォルダに、機能を一通り使った動くサンプルを用意しています（[samples/README.md](./samples/README.md)）。

| ファイル | 内容 |
| :--- | :--- |
| `guess.bas` | 数当てゲーム（`INPUT` / `IF`〜`ELSE` / 行ラベル / `RND`） |
| `art.bas` | 幾何アート（`POLY` / `CIRCLE` / `PAINT`） |
| `ball.bas` | 跳ねるボール（`GET@` / `PUT@` スプライト） |
| `draw.bas` | タッチでお絵かき（`TOUCH()` / `PSET`） |
| `song.bas` | 演奏＋ビジュアライザ（非同期 `PLAY` ＋描画） |
| `sfx.bas` | PSG 効果音（`SOUND` のノイズ・エンベロープ） |
| `qix.bas` | 陣取りゲーム（クイックス風。配列＋フラッドフィル） |

---

## 9. 実装状況（開発者向け）

命令ごとの Parser / HAL の状態は [docs/COMMAND_STATUS.md](./docs/COMMAND_STATUS.md) で管理しています（`specification.md` と併せて参照してください）。
